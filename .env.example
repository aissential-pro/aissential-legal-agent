# AIssential Legal Agent - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# AI CONNECTOR CONFIGURATION (Optional)
# =============================================================================

# AI Connector base URL for centralized credential management
# The Connector stores credentials (Telegram, Google, etc.) so they only
# need to be configured once across all AIssential apps
CONNECTOR_BASE_URL=https://connector.aissential.pro/v1

# Your Connector API key for authenticated access (optional)
# If not set, the app falls back to individual service env vars below
CONNECTOR_API_KEY=conn_xxx

# =============================================================================
# LLM GATEWAY CONFIGURATION
# =============================================================================

# Gateway API base URL
GATEWAY_BASE_URL=https://gateway.aissential.pro/v1

# Your Gateway API key for authenticated access
# Obtain from your AIssential Gateway admin
GATEWAY_API_KEY=gw_live_xxx

# Client identifier for Gateway requests
# Default: aissential-internal
GATEWAY_CLIENT_ID=aissential-internal

# =============================================================================
# GOOGLE DRIVE CONFIGURATION
# =============================================================================

# The Google Drive folder ID to scan for contracts
# Find this in the URL when viewing a folder: drive.google.com/drive/folders/{FOLDER_ID}
GOOGLE_DRIVE_FOLDER_ID=your_google_drive_folder_id_here

# Path to your Google Cloud Platform service account JSON credentials file
# Download from: Google Cloud Console > IAM & Admin > Service Accounts
GCP_CREDENTIALS_PATH=gcp.json

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Directory where log files will be stored
# Default: logs (relative to project root)
LOGS_DIR=logs

# Path to the file tracking processed documents
# Default: processed.json (relative to project root)
PROCESSED_FILE=processed.json

# Enable debug mode for verbose logging (true/false)
# Default: false
DEBUG_MODE=false
